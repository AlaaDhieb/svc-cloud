/**
 * Liste des constantes/configs utilisées dans le projet.
 * @type {{collections: {movies: string, comments: string}, samples: {mflix: string}}}
 */
export const MongoConfig = {
    collections: {
        movies: "movies",
        comments: "comments"
    },

    samples: {
        mflix: "sample_mflix"
    }

};